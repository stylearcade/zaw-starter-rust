{
  "name": "zaw-starter-rust",
  "version": "0.1.0",
  "private": true,
  "description": "zaw starter template with Rust WASM and TypeScript",
  "scripts": {
    "install": "./rust/install.sh && npm install --prefix typescript",
    "build": "./rust/build.sh npm run build --prefix typescript",
    "test": "npm run build && npm run test --prefix typescript",
    "bench": "npm run build && npm run bench --prefix typescript"
  }
}
